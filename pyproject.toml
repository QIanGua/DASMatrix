[project]
name = "dasmatrix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.11.5",
]

[tool.ruff]
line-length = 88 # 示例配置

[tool.ruff.lint]
select = ["E", "F", "W", "I"] # E=pycodestyle errors, F=pyflakes, W=pycodestyle warnings, I=isort

[tool.ruff.format]
quote-style = "double"